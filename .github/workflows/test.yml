name: test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.12 # 2023-2024
          cache: 'pip' # caching pip dependencies
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest nbconvert ipykernel scikit-learn scipy matplotlib validate-pyproject trove-classifiers coverage
      # parse libraries and run doctests:
      - run: PYTHONPATH=src python oncp/classifiers.py
      - run: PYTHONPATH=src python oncp/CPS.py
      - run: PYTHONPATH=src python oncp/CRR.py
      - run: PYTHONPATH=src python oncp/kernels.py
      - run: PYTHONPATH=src python oncp/martingale.py
      # run our notebooks
      - run: PYTHONPATH=src jupyter-nbconvert --execute example.ipynb --to html
      - run: PYTHONPATH=src jupyter-nbconvert --execute test_exchangeability.ipynb --to html
      # validate the pyproject.toml
      - run: validate-pyproject pyproject.toml
      # TODO: run: pytest once tests are added
      - name: Code coverage quick report
        run: |
          export PYTHONPATH=src
          coverage erase
          coverage run -a oncp/CRR.py
          coverage run -a oncp/classifiers.py
          coverage run -a oncp/CPS.py
          coverage run -a oncp/kernels.py
          coverage run -a oncp/martingale.py
          coverage report
